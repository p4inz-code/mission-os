/// @file
/// QML plugin implementation for the org.mission.ui module.
///
/// All design tokens and theme components are self-registering via
/// QML files in the org.mission.ui module directory. The C++ plugin
/// only needs to exist to make the module discoverable by the Qt Quick
/// engine. No manual type registration is required.

#include "mission/missionuiplugin.h"
